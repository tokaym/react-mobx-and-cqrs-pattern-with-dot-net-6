export interface Estimate {
      materialSKU: string,
      materialName: string,    
      cd: string,
      estimate: number,
      order: number,
      sent: number,
      stock: number,
      stockScore: string
}
